# M2 验收清单（实测结果）

对照设计文档 §2.3 的成功标准逐条验证。**全部在真机上完成**，用 `adb` 驱动 + 截图取证。

设备：Honor BVL-AN16 / Android 16 / Expo Go 57.0.9 / SDK 57
验证方式：`adb shell input tap|swipe` 驱动 + `adb shell screencap` 截图

---

## 成功标准 1：一组做完到记录完成 ≤ 2 次点击

- [x] 进入记录界面后，点「✓ 完成这组」即完成记录（**1 次点击**）
- [x] 点「开始下一组」回到记录态（**1 次点击**）
- [x] 全程无需手动输入数字（数值预填上一组）
- [x] 数值可用**拖动**调整，不必一下下点加减

**证据**：连点「完成这组」→「开始下一组」，组号 1 → 2 → 3 递增；拖 420 物理像素后 22.5 → 45 kg（+9 档，符合 12dp/档的设计值）。

---

## 成功标准 2：记录过程不打断训练节奏

- [x] 训练期间屏幕保持常亮（`keepScreenAwake`）
- [x] 完成一组有轻微震动反馈（`expo-haptics`）
- [x] 组间休息是**正计时**，不催促
- [x] 换动作不必退出记录界面（顶部动作条 + `＋` 弹层）

**证据**：休息界面从 `00:03` 正常起算；动作条可切换「深蹲 / 下斜卧推」，切换后数值按动作正确恢复。

---

## 成功标准 3：训练记录永不丢失 ⭐

- [x] 完成 3 组后强杀 App，重开后「继续上次训练」出现
- [x] 进入后已完成的组数正确
- [x] **息屏挂起后计时仍然准确**

**证据（本轮最关键的验证）**：

| 检查项 | 结果 |
|---|---|
| 完成 2 组后强杀 App → 重开 | 组号正确保留（第 3 组） |
| 息屏前计时 | `18:20` |
| 息屏 35 秒 + 亮屏后 | **`19:01`** |
| Δ 与实测等待时间 | 41 秒 vs 40 秒，**吻合** |

最后一条证明了设计文档 §9 的硬约束：**计时基于 `rest_started_at` 时间戳，不是 `setInterval` 累加计数器**。若用计数器，息屏挂起时它会冻住不动。

---

## 成功标准 4：训练结束后能看到次数对比与粗略提示

- [x] 顶部三个数字：**2 分钟 / 2 总组数 / 320 总容量 kg**
- [x] **卡片第一行是原始次数数组**（`深蹲　8 / 8`）
- [x] 提示文案与次数数组一致
- [x] 页面标注了**「粗略参考」**（区块标题：「组间休息回顾（粗略参考）」）
- [x] 少于 3 组显示**「组数不足 3 组，暂不判断」**

**手工核算**：2 组 × (20 kg × 8 次) = **320 kg**，与页面显示一致。

---

## 数据落地检查

- [x] 点「保存这次训练」后回到训练页
- [x] **训练页不再显示「继续上次训练」**（`reset()` 生效）
- [x] 重启 App 后无残留进行中的训练
- [x] 从零建库：清空 App 数据后重新打开，4 张表 + 52 个动作全部建出

---

## 真机验证才能发现的问题（单元测试照不到）

这四条全部是**只有真机使用才会暴露**的，且都不在原始计划里：

| # | 问题 | 性质 |
|---|---|---|
| 1 | **换不了动作** —— 一次训练只能练一个动作 | **功能性缺口**，导致 App 无法完成任何一次真实训练 |
| 2 | 隐藏导航栏后顶部动作条压住状态栏 | 安全区遗漏 |
| 3 | 加减按钮调数值太低效 | 交互效率 |
| 4 | 组数只是右上角小灰字，不显眼 | 信息层级 |
| 5 | 完成一组后若一直不按「开始下一组」，`rest_started_at` 会一直留着 —— 重开 App 被堵在休息页，且这段荒唐时长会污染休息数据 | **数据质量 + 可用性** |

第 5 条补了 30 分钟闸门（`domain/rest.ts` + `setRepo.cancelRest` + `store.loadExercises`），并首次给 **store 层**补上集成测试（此前 79 个测试全在 domain/repository，中间那层编排是空白）。

---

## 结论

**M2 达成。这个 App 现在可以真的带去健身房用了。**

---

# M3 历史记录 验收结果

真机验证（adb 驱动 + 截图取证），全部通过：

| 检查项 | 结果 |
|---|---|
| 练完一场并保存后，切到「历史」标签能看到这条记录 | ✅ |
| 列表行显示日期 / 名称 / 时长 / 组数 / 容量 | ✅ `9月16日 周三` / `未命名训练` / `1 分钟 · 1 组 · 160 kg` |
| 数字与手工核算一致 | ✅ 1 组 × (20kg × 8) = **160 kg** |
| 点进详情，次数数组原样显示 | ✅ `下斜卧推　8` |
| 区块标注「粗略参考」 | ✅ 标题即「组间休息回顾（粗略参考）」 |
| 不足 3 组时明确沉默 | ✅ 「组数不足 3 组，暂不判断」 |
| 详情页**没有**「保存这次训练」按钮 | ✅ |
| 详情页有原生导航栏（自带返回与安全区） | ✅ 「训练详情」 |

---

# 真机使用后修掉的问题（M3 之后）

| # | 问题（用户提出） | 根因 | 处理 |
|---|---|---|---|
| 6 | **每次训练都以「深蹲」开始** | `startNew` 里写死了 `all.find(e => e.name === '深蹲')` —— 等于假设每个人都从深蹲开始 | 改为复制上一次**已结束**训练的动作组合与顺序；没有历史时开一场空训练 |
| 7 | （连带发现）空训练是条死路 | 空状态只显示一句「这次训练还没有动作」，**没有任何添加入口** | 补上整宽「＋ 添加动作」按钮，复用同一个动作选择弹层，空状态同样让出安全区 |

**附带修掉的两个隐患**（都不是用户提的，是顺着查出来的）：

- **复制动作时若不预建第一组**，记录界面就找不到「待完成的组」，`completeCurrentSet` 会直接 return —— **用户点「完成这组」会静默无反应**。抽了 `addExerciseWithFirstSet` 让 `startNew` 与 `addExercise` 共用。
- **我自己写的一条测试是赌时钟的**：`expect(second.startedAt).toBe(first.startedAt)` 假设两次 `createSession` 落在同一毫秒。实测 **20 次全量闪 2 次**（差 1 毫秒）。改为显式把两行时间戳设成同一个值，让并列成为确定事件 —— 测的是 SQL 排序语义本身，而不是运气。改后 **25 次 0 失败**。

> 教训：**一条会随机变红的测试，比没有测试更危险** —— 它会训练所有人忽略红色。

---

## 尚未做的（有意保留）

| | 为什么 |
|---|---|
| **M4 进步曲线** | 需要真实训练数据才有意义。现在做是在猜用户想问什么 |
| **M5 导出备份** | ⚠️ **目前最该优先的一项**：数据只存在一台手机里，丢了就没了 |
| v1.5 训练模板 / 有氧 / 体重 | 按计划推迟 |
