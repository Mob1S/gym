# M3 历史记录 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户能翻看过去的每一次训练 —— 列表看概览，点进去看那天的每一组和组间休息回顾。

**Architecture:** 仓储层加一个**聚合查询**一次取回列表所需的全部字段（避免 N+1）；把总结页的内容抽成可复用组件 `SessionSummaryView`，训练总结页与历史详情页共用同一套渲染。

**Tech Stack:** Expo SDK 57 / React Native / TypeScript / expo-router / expo-sqlite（生产）/ node:sqlite（测试）

## Global Constraints

沿用 M1+M2 计划的全部约束（见 `2026-09-15-gym-tracker-m1-m2.md` 的 `## Global Constraints`），要点：

- **分层硬规则**：`app/` 与 `src/components/` 永远不直接 import `src/db/`，不写 SQL；数据访问只能经过 `src/repositories/`。
- **测试必须带 `--runInBand`**（沙箱禁止子进程管道 stdio，默认 `spawn EPERM`）。
- **读中文文件用 read 工具**，不要用 `Get-Content`（显示乱码是假象）。
- **`SqlExecutor.run()` 在多语句 SQL 上必须走 exec 语义**（`prepare()` 只执行第一条，静默丢表）。
- **所有 `ORDER BY started_at DESC` 必须补 `rowid DESC` 兜底**（毫秒会撞车，并列时顺序不确定）。
- **容器无 `.git` 覆盖风险**：`create-expo-app` 会 `git init`，搬文件时必须排除 `.git`。
- **动态路由导航必须用对象形式**：`{ pathname: '/history/[id]', params: { id } }`。SDK 57 的 `typedRoutes` 只生成字面量，模板字符串过不了 tsc。
- **隐藏导航栏的页面必须用 `useSafeAreaInsets()` 自己让出状态栏。**
- **RN 0.86.3 类型里没有 `StyleSheet.absoluteFillObject`**，只有 `absoluteFill`。
- 所有面向用户的文案是简体中文。
- **e1RM 在 M3 不显示**（属于 M4）。

---

## 文件结构

```
app/
  (tabs)/history.tsx          改写：从占位页变成训练历史列表
  history/[id].tsx            新建：某一次训练的详情
  session/summary/[id].tsx    改：改为复用 SessionSummaryView
src/
  components/SessionSummaryView.tsx   新建：统计 + 组间休息回顾，训练总结页与历史详情页共用
  repositories/sessionRepo.ts         改：加 listSessionSummaries 聚合查询
  repositories/sessionRepo.summaries.test.ts  新建
```

---

### Task 1: 仓储层聚合查询

**Files:**
- Modify: `src/repositories/sessionRepo.ts`
- Test: `src/repositories/sessionRepo.summaries.test.ts`

**Interfaces:**
- Consumes: `SqlExecutor`、`newId()`、已有仓储函数
- Produces:
  - `interface SessionSummary { id: string; name: string | null; startedAt: number; finishedAt: number; durationMinutes: number; setCount: number; volumeKg: number }`
  - `listSessionSummaries(exec: SqlExecutor, limit: number): Promise<SessionSummary[]>`

- [ ] **Step 1: 写失败的测试**

`src/repositories/sessionRepo.summaries.test.ts`：

```ts
import { createMigratedExecutor } from '../db/__tests__/nodeExecutor';
import { createCustomExercise } from './exerciseRepo';
import {
  addExerciseToSession,
  createSession,
  finishSession,
  listSessionSummaries,
} from './sessionRepo';
import { addSet, completeSet } from './setRepo';

const MINUTE = 60 * 1000;

/** 造一场已结束的训练，含若干已完成组 */
async function makeFinishedSession(
  exec: Awaited<ReturnType<typeof createMigratedExecutor>>,
  label: string,
  startedAt: number,
  finishedAt: number,
  sets: { weight: number; reps: number; completed: boolean }[],
) {
  const session = await createSession(exec, label);
  // createSession 用 Date.now() 写 started_at，这里覆写成测试指定值
  await exec.run('UPDATE session SET started_at = ? WHERE id = ?', [
    startedAt,
    session.id,
  ]);

  const exercise = await createCustomExercise(exec, `动作-${label}`, '腿', '杠铃');
  const se = await addExerciseToSession(exec, session.id, exercise.id);
  for (const s of sets) {
    const set = await addSet(exec, se.id, s.weight, s.reps);
    if (s.completed) await completeSet(exec, set.id, startedAt);
  }
  await finishSession(exec, session.id, finishedAt);
  return session.id;
}

describe('listSessionSummaries', () => {
  it('没有历史时返回空数组', async () => {
    const exec = await createMigratedExecutor();
    expect(await listSessionSummaries(exec, 10)).toEqual([]);
  });

  it('进行中的训练不出现在历史里', async () => {
    const exec = await createMigratedExecutor();
    await createSession(exec, '还没练完');
    expect(await listSessionSummaries(exec, 10)).toEqual([]);
  });

  it('返回时长、组数与容量', async () => {
    const exec = await createMigratedExecutor();
    const t0 = 1_700_000_000_000;
    await makeFinishedSession(exec, '腿部日', t0, t0 + 52 * MINUTE, [
      { weight: 100, reps: 5, completed: true },
      { weight: 100, reps: 5, completed: true },
      { weight: 100, reps: 3, completed: true },
    ]);

    const [row] = await listSessionSummaries(exec, 10);
    expect(row.name).toBe('腿部日');
    expect(row.durationMinutes).toBe(52);
    expect(row.setCount).toBe(3);
    expect(row.volumeKg).toBe(1300);
  });

  it('未完成的组不计入组数与容量', async () => {
    const exec = await createMigratedExecutor();
    const t0 = 1_700_000_000_000;
    await makeFinishedSession(exec, '腿部日', t0, t0 + 10 * MINUTE, [
      { weight: 100, reps: 5, completed: true },
      { weight: 100, reps: 5, completed: false },
    ]);

    const [row] = await listSessionSummaries(exec, 10);
    expect(row.setCount).toBe(1);
    expect(row.volumeKg).toBe(500);
  });

  it('一个动作都没有的训练返回 0 组 0 容量，而不是被漏掉', async () => {
    const exec = await createMigratedExecutor();
    const session = await createSession(exec, '空训练');
    await finishSession(exec, session.id, Date.now());

    const rows = await listSessionSummaries(exec, 10);
    expect(rows).toHaveLength(1);
    expect(rows[0].setCount).toBe(0);
    expect(rows[0].volumeKg).toBe(0);
  });

  it('按开始时间倒序，最新的在最前', async () => {
    const exec = await createMigratedExecutor();
    const t0 = 1_700_000_000_000;
    await makeFinishedSession(exec, '早的', t0, t0 + MINUTE, []);
    await makeFinishedSession(exec, '晚的', t0 + 60 * MINUTE, t0 + 61 * MINUTE, []);

    const rows = await listSessionSummaries(exec, 10);
    expect(rows.map((r) => r.name)).toEqual(['晚的', '早的']);
  });

  it('同毫秒开始的两次训练顺序仍然确定（rowid 兜底）', async () => {
    const exec = await createMigratedExecutor();
    const t0 = 1_700_000_000_000;
    await makeFinishedSession(exec, '先建', t0, t0 + MINUTE, []);
    await makeFinishedSession(exec, '后建', t0, t0 + MINUTE, []);

    const rows = await listSessionSummaries(exec, 10);
    expect(rows.map((r) => r.name)).toEqual(['后建', '先建']);
  });

  it('尊重 limit', async () => {
    const exec = await createMigratedExecutor();
    const t0 = 1_700_000_000_000;
    for (let i = 0; i < 4; i++) {
      await makeFinishedSession(exec, `第${i}场`, t0 + i * MINUTE, t0 + (i + 1) * MINUTE, []);
    }
    expect(await listSessionSummaries(exec, 2)).toHaveLength(2);
  });

  it('多个动作的容量会累加，不会因为 JOIN 翻倍', async () => {
    const exec = await createMigratedExecutor();
    const t0 = 1_700_000_000_000;
    const session = await createSession(exec, '两个动作');
    await exec.run('UPDATE session SET started_at = ? WHERE id = ?', [t0, session.id]);

    for (const name of ['深蹲', '腿举']) {
      const ex = await createCustomExercise(exec, name, '腿', '杠铃');
      const se = await addExerciseToSession(exec, session.id, ex.id);
      const set = await addSet(exec, se.id, 100, 5);
      await completeSet(exec, set.id, t0);
    }
    await finishSession(exec, session.id, t0 + MINUTE);

    const [row] = await listSessionSummaries(exec, 10);
    expect(row.setCount).toBe(2);
    expect(row.volumeKg).toBe(1000);
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

```powershell
npx jest --runInBand src/repositories/sessionRepo.summaries.test.ts
```

Expected: FAIL —— `listSessionSummaries is not a function`（或找不到导出）。

- [ ] **Step 3: 写实现**

在 `src/repositories/sessionRepo.ts` 末尾追加：

```ts
export interface SessionSummary {
  id: string;
  name: string | null;
  startedAt: number;
  /** 已结束的训练，finishedAt 一定非空 */
  finishedAt: number;
  durationMinutes: number;
  setCount: number;
  /** 容量负荷（volume load）= Σ(重量 × 次数)，只算已完成的组 */
  volumeKg: number;
}

interface SessionSummaryRow {
  id: string;
  name: string | null;
  started_at: number;
  finished_at: number;
  set_count: number;
  volume_kg: number;
}

/**
 * 一次查询取回历史列表所需的全部字段。
 *
 * 用聚合而不是「先查训练、再逐个查组」，是因为列表页要显示几十场训练 ——
 * N+1 查询在手机上会肉眼可见地卡。
 *
 * `ORDER BY started_at DESC, rowid DESC`：`started_at` 是毫秒时间戳，同毫秒
 * 建的两场训练会完全并列，此时 SQLite 按扫描顺序返回（即先建的在前），
 * 语义就反了。rowid 即插入顺序，用它兜底才有确定结果。
 */
export async function listSessionSummaries(
  exec: SqlExecutor,
  limit: number,
): Promise<SessionSummary[]> {
  const rows = await exec.all<SessionSummaryRow>(
    `SELECT
       s.id                AS id,
       s.name              AS name,
       s.started_at        AS started_at,
       s.finished_at       AS finished_at,
       COUNT(CASE WHEN st.is_completed = 1 THEN 1 END)                        AS set_count,
       COALESCE(SUM(CASE WHEN st.is_completed = 1 THEN st.weight * st.reps END), 0) AS volume_kg
     FROM session s
     LEFT JOIN session_exercise se ON se.session_id = s.id
     LEFT JOIN set_entry st        ON st.session_exercise_id = se.id
     WHERE s.finished_at IS NOT NULL
     GROUP BY s.id
     ORDER BY s.started_at DESC, s.rowid DESC
     LIMIT ?`,
    [limit],
  );

  return rows.map((row) => ({
    id: row.id,
    name: row.name,
    startedAt: row.started_at,
    finishedAt: row.finished_at,
    durationMinutes: Math.max(
      0,
      Math.round((row.finished_at - row.started_at) / 60000),
    ),
    setCount: Number(row.set_count),
    volumeKg: Number(row.volume_kg),
  }));
}
```

- [ ] **Step 4: 运行测试，确认通过**

```powershell
npx jest --runInBand src/repositories/sessionRepo.summaries.test.ts
```

Expected: PASS，9 个测试全绿。

- [ ] **Step 5: 提交**

```powershell
git add src/repositories
git commit -m "feat(repo): 历史列表的会话汇总聚合查询"
```

---

### Task 2: 抽出可复用的训练回顾组件

**Files:**
- Create: `src/components/SessionSummaryView.tsx`
- Modify: `app/session/summary/[id].tsx`

**Interfaces:**
- Consumes: `listSessionExercises`、`listSets`、`getExercise`、`getSession`、`totalVolumeLoad`、`buildRestFeedback`
- Produces: `SessionSummaryView({ sessionId }: { sessionId: string })` —— 渲染三个统计 + 「组间休息回顾（粗略参考）」区块

- [ ] **Step 1: 读现有的 `app/session/summary/[id].tsx`**

把它的渲染逻辑原样搬到新组件里，**不要改动任何文案与样式**：

- 三个统计：分钟 / 总组数 / 总容量 kg
- 区块标题必须含**「粗略参考」**
- 每张卡片第一行是**动作名 + 原样次数数组**（`repsList.join(' / ')`）
- `buildRestFeedback` 返回 `null` 时显示「组数不足 3 组，暂不判断」
- 没有已完成组时显示「这次训练还没有完成的组」
- `loaded` 标志避免加载中闪出空态文案

`app/session/summary/[id].tsx` 改成：

```tsx
// 只保留「保存这次训练」按钮与它的跳转逻辑
<SessionSummaryView sessionId={id} />
<Pressable ...>保存这次训练</Pressable>
```

**保存按钮的行为一个字都不要改**（`endWorkout` → `reset()` → `router.replace('/(tabs)')`）。

- [ ] **Step 2: 验证没坏**

```powershell
npx tsc --noEmit
npx jest --runInBand
```

Expected: tsc 退出码 0；测试仍是 10 suites / 79 tests 全绿。

- [ ] **Step 3: 提交**

```powershell
git add -A
git commit -m "refactor: 抽出 SessionSummaryView，供训练总结页与历史详情页共用"
```

---

### Task 3: 历史列表页

**Files:**
- Modify: `app/(tabs)/history.tsx`

**Interfaces:**
- Consumes: `useDatabase()`、`listSessionSummaries(exec, limit)`、`useFocusEffect`
- Produces: 训练历史列表

- [ ] **Step 1: 实现列表**

要求：

- 用 `listSessionSummaries(exec, 50)` 取数
- 每一行显示：**日期**（如 `9月16日 周三`）、训练名（没有就显示「未命名训练」）、`52 分钟 · 18 组 · 8,240 kg`
- **每次进入这个标签页都要重新取数**（用 expo-router 的 `useFocusEffect`），否则刚练完保存回来还看不到新记录
- 空状态显示「还没有训练记录」+ 一句引导（「去『训练』标签开始第一次吧」）
- 点击某一行 → `router.push({ pathname: '/history/[id]', params: { id } })`（**对象形式**）
- 列表用 `FlatList`，行与行之间留分隔线
- 遵守安全区

- [ ] **Step 2: 验证**

```powershell
npx tsc --noEmit
npx jest --runInBand
```

Expected: tsc 退出码 0；测试 10 suites / 79 tests 全绿。

- [ ] **Step 3: 提交**

```powershell
git add -A
git commit -m "feat: 历史列表页"
```

---

### Task 4: 历史详情页

**Files:**
- Create: `app/history/[id].tsx`
- Modify: `app/_layout.tsx`

**Interfaces:**
- Consumes: `SessionSummaryView`
- Produces: 单次训练详情路由

- [ ] **Step 1: 实现**

要求：

- 用 `useLocalSearchParams` 取 `id`，渲染 `<SessionSummaryView sessionId={id} />`
- 顶部显示这次训练的名字与日期（读 `getSession`）
- **不要有「保存这次训练」按钮**（这是已经结束的训练）
- 在 `app/_layout.tsx` 加 `<Stack.Screen name="history/[id]" options={{ title: '训练详情' }} />`，或按需要设 `headerShown: false` 并自己用 `useSafeAreaInsets` 让出状态栏 —— **选一种，别两边都做**
- 用 `ScrollView` 保证内容可滚动

- [ ] **Step 2: 验证**

```powershell
npx tsc --noEmit
npx jest --runInBand
```

Expected: tsc 退出码 0；测试 10 suites / 79 tests 全绿。

- [ ] **Step 3: 提交**

```powershell
git add -A
git commit -m "feat: 历史详情页"
```

---

### Task 5: M3 真机验收

**Files:**
- Modify: `docs/superpowers/plans/m2-acceptance-checklist.md`（追加 M3 一节）

- [ ] **Step 1: 确认全量测试与类型检查**

```powershell
npx jest --runInBand
npx tsc --noEmit
```

Expected: 11 suites / 88 tests 全绿；tsc 退出码 0。

- [ ] **Step 2: 真机逐条验收**（由主 agent 用 adb 截图完成）

- [ ] 练完一次训练并保存后，切到「历史」标签能看到这条记录
- [ ] 列表行显示日期 / 名称 / 时长 / 组数 / 容量，且与训练时实际相符
- [ ] 点进详情，次数数组原样显示、区块标注「粗略参考」
- [ ] 详情页**没有**「保存这次训练」按钮
- [ ] 空库时显示「还没有训练记录」
- [ ] 从历史返回、再进入，数据仍然正确（不串行、不重复）

- [ ] **Step 3: 提交**

```powershell
git add -A
git commit -m "docs: M3 验收结果"
```

---

## 完成之后

M3 达成即「翻看过去的训练」可用。剩下的：

- **M4 进步曲线** —— 建议等积累若干次真实训练数据后再做，否则是在猜用户想问什么
- **M5 导出备份**
- **M7 出独立 APK** —— **这是目前最大的现实缺口**：App 现在只在插着电脑 USB 时能跑，没法带去健身房
